DATABASE_URL=postgresql+psycopg://northend:northend@localhost:5433/northend
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000
RAW_MENU_DIR=data/raw_menus

# Client — Mapbox (browser only, restrict by URL in Mapbox dashboard)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Client
NEXT_PUBLIC_API_URL=http://localhost:8001

# Server-side only — never expose to the Next.js client.
# Restaurant hero: rating, review count, price level, open/closed, hours.
# GOOGLE_MAPS_API_KEY=

# Review-intelligence summarization (not built yet — needs Places reviews too).
# GEMINI_API_KEY=

# Restaurant hero: estimated wait + weekly popularity chart.
# BESTTIME_API_KEY=
