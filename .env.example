DATABASE_URL=postgresql+psycopg://northend:northend@localhost:5433/northend
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3001
RAW_MENU_DIR=data/raw_menus

# Client — Mapbox (browser only, restrict by URL in Mapbox dashboard)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Client
NEXT_PUBLIC_API_URL=http://localhost:8001

# Server-side only — never expose to the Next.js client.
# Places key must stay server-side and be restricted to the Places API.
# GOOGLE_MAPS_API_KEY=
# GOOGLE_PLACE_PHOTOS_ENABLED=false
# GOOGLE_PLACE_PHOTO_MONTHLY_CAP=900

# Menu extraction: scripts/extract_menu.py turns a pending snapshot into
# menu_items (held for review — see scripts/review_extraction.py). Also
# needed later for review-intelligence summarization (not built yet).
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash

# Restaurant hero: live/weekly/hourly crowd data via SerpApi's Google Maps
# Place Results API (Google Popular Times). Get a key at serpapi.com.
# SERPAPI_KEY=
